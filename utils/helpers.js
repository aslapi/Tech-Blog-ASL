// const Handlebars = require('handlebars');

// const isLoggedIn = function(user, options) {
//     return user ? options.fn(this) : options.inverse(this);
// };

// Handlebars.registerHelper('ifLoggedIn', isLoggedIn);

// const helpers = {
//     ifLoggedIn: isLoggedIn,
// };

// module.exports = helpers;